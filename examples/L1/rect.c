#include<stdio.h>

int main()
{
    int length = 3;
    int width = 4;
    int area = length * width;
    printf("Area of Rectangle with sides %d, %d is %d\n", length, width, area);
    
    return 0;
}
